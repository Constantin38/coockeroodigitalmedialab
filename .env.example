# Provider selection.
# "mock" is the default and makes no external or billable request.
GENERATION_PROVIDER=mock

# Required only when GENERATION_PROVIDER=higgsfield.
# Keep this server-side and never commit .env.local.
HF_CREDENTIALS=KEY_ID:KEY_SECRET
